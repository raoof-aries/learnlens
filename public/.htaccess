<IfModule mod_rewrite.c>
  RewriteEngine On

  # Handle SPA Client-Side Routing for Apache
  # Works dynamically in root directory or any subfolder
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . index.html [L]
</IfModule>
